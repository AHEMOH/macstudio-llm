# macOS metadata
.DS_Store

# Local editor folders
.vscode/
.claude/

# Project docs not part of shipped repo
CLAUDE.md

# Python build artefacts
__pycache__/
*.pyc

# Rendered logs that might appear during local testing
/var/log/macstudio/

# Local test documents (may contain private data — never commit)
# All root-level PDFs are treated as private test scans, never shipped in this repo.
/*.pdf

